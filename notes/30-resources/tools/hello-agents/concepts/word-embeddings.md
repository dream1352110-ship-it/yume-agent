# 词向量与余弦相似度

词向量把 Token 映射为可计算的连续向量。余弦相似度比较两个向量的方向：`cos(a,b) = a·b / (|a||b|)`，越接近 `1` 通常表示在当前表示空间中越相近。

向量关系可以表达一定的语义方向，例如 `king - man + woman ≈ queen`；这不是固定规则，而是训练后表示空间中的近似结构。

代码：[chapter3/demo2.py](../../../../../code/chapter3-demo2.md)。
